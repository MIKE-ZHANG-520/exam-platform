# DESIGN.md — 智慧培训考试平台

## 气质与意象

**意象锚点**：现代化企业数字工作台 · 科技蓝晨光工作站
- 场景：早晨走进敞亮的智能办公楼，落地窗前的显示屏上跳动着蓝色数据仪表。既有政企平台的专业与秩序，又不失互联网产品的呼吸感与温度。
- 关键词：**专业、清爽、现代、可信赖**。
- 管理端像"新一代数字驾驶舱"，工人端像"随身培训助手"。

## 视觉策略

- **图标语言**：lucide 线性图标 1.5px 描边，配置到与主色协调的容器背景（浅蓝底、白色描线）。
- **图形装饰**：允许少量渐变（导航栏、Hero、KPI 图标容器）；模糊光斑（blur-3xl opacity-20）作为背景装饰，用量克制。
- **禁忌**：3D emoji、粒子特效、大量玻璃拟态、超大阴影模糊。

## 配色方案（科技蓝 + 中性灰）

| Token | 色值 | 用途 |
|-------|------|------|
| `--primary` | `#1677ff` | 主色（按钮、导航高亮、关键数值） |
| `--primary-dark` | `#0958d9` | hover/active |
| `--primary-light` | `#4096ff` | 强调/进度条 |
| `--primary-50` | `#e6f4ff` | 浅蓝背景 |
| `--sidebar-from` | `#0b3d91` | 侧边栏渐变起点（深蓝） |
| `--sidebar-to` | `#1677ff` | 侧边栏渐变终点 |
| `--bg` | `#f5f7fa` | 页面背景 |
| `--card` | `#ffffff` | 卡片 |
| `--border` | `#e5e7eb` | 分隔线 |
| `--fg` | `#1f2937` | 标题 |
| `--fg-body` | `#4b5563` | 正文 |
| `--fg-muted` | `#9ca3af` | 辅助/占位 |
| `--success` | `#10b981` | 通过 |
| `--warning` | `#f59e0b` | 待补考 |
| `--danger` | `#ef4444` | 不及格/切屏警告 |
| `--purple` | `#8b5cf6` | 互动设问 |
| `--orange` | `#f97316` | 常见误区 |

同时提供状态化背景色 token：`--success-bg #ecfdf5`、`--warning-bg #fffbeb`、`--danger-bg #fef2f2`、`--purple-bg #f5f3ff`、`--orange-bg #fff7ed`，用于卡片/徽标/callout。

## 字体排版

- 中文：`PingFang SC`, `Microsoft YaHei` 系统栈。
- 数字：`tabular-nums`，KPI 大字 30-36 / 700，倒计时 28 / 700。
- 字号节奏：H1 24/600、H2 18/600、卡片标题 16/600、正文 14/400、辅助 12/400。
- 行高：标题 1.3、正文 1.6。

## 布局与响应式

- **管理后台**：左侧固定侧边栏（240px，渐变蓝背景），顶部条含面包屑 + 用户头像下拉。主内容 max-w-[1440px]、padding 24-32px。断点 <1280px 主内容 padding 缩到 16px；<1024px 侧边栏可收起为图标。
- **工人端 H5**：单列 max-w-[480px] 居中，页面 padding 16px；顶部 sticky 渐变标题栏（品牌蓝渐变）+ 白色文字。按钮 h-12 全宽。
- **卡片圆角**：小卡片 8px；大容器 12px；按钮/输入框 8px。
- **卡片阴影**：静态 `0 2px 8px rgba(0,0,0,0.06)`；hover `0 4px 16px rgba(0,0,0,0.10)`；过渡 200ms。

## 组件规范

- **按钮**：主操作 filled 主蓝，圆角 8px，h-10；hover 主蓝-dark；loading 内嵌 spinner。次操作 outline；危险 destructive 红。
- **KPI 卡片**：`{图标容器渐变彩底}` + 数字 + 标签，图标容器 rounded-xl，尺寸 48px。不同 KPI 用不同色卡（蓝/绿/橙/紫/红）。
- **Badge**：圆角 6px，通过绿、警告橙、危险红、信息蓝、灰色四种预设。
- **Table**：斑马纹关闭，行高 52px，hover `--primary-50` 弱蓝，无外框，仅横向分隔线。
- **输入框**：h-10、圆角 8px、focus 时 border 主蓝 + `box-shadow 0 0 0 3px rgba(22,119,255,0.15)`。

## 交互与动效

- 全局过渡统一 `duration-200 ease-out`；卡片 hover 上浮 `-translate-y-0.5` + 阴影加深。
- 页面切换 fade-in 200ms；列表进入错落淡入（Skeleton loader → 内容渐显）。
- 按钮 hover：主按钮亮度+2%，次按钮背景色轻微填充。
- 数字变化用 tabular-nums 保持稳态，无跳动。
- 禁止弹跳、缩放旋转、视差滚动、鼠标跟随光晕。

## 提纲组件化排版

工人版按 chunk 类型渲染：
- ✅ 正确做法 → 绿底浅卡片 + 勾选图标
- ⚠️ 注意事项 → 橙底浅卡片 + 警示图标
- 🔴 禁止行为 → 红底浅卡片 + 禁止图标
- 💡 小口诀 → 米黄底 + 左侧橙色边条 + 引用样式
- 重点 → 主蓝浅底高亮框
- 数字编号 → 主蓝圆形底 + 白色数字

培训师版按段落类型渲染：
- 核心知识点 → 蓝色卡片
- 常见误区 → 橙色警告卡片
- 互动设问 → 紫色对话气泡（左上有小三角）
- 时间分配 → 绿色进度条卡片

## 空态与骨架

- 空态：SVG 插画（简笔线条）+ 主标题（14/500）+ 副说明（12/400 灰）+ 主行动按钮。
- 骨架屏：灰色 pulse 条 + 主色 4% 高亮；列表 3-5 行占位。

## 可访问性与文案

- 主文字对比度 ≥ 7:1。工人端文案口语化（"开始答题"、"再来一次"）；管理端可用专业词。
- 表单错误信息红色 12px 紧贴输入框下方；必填项 label 后加 `*`。
- 移动端触控最小 44×44px。

## 明确禁忌

- ❌ 不要暗色主题作为默认。
- ❌ 不要大圆角胶囊按钮（>12px）。
- ❌ 不要用 emoji 代替状态图标（emoji 仅在提纲展示的语义标签里使用）。
- ❌ 不要用大面积主色平铺整个页面背景。
- ❌ 不要在工人端出现 "KPI、率、指标" 等 B 端词汇。
