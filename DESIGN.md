# DESIGN.md — 智慧培训考试平台

## 气质与意象

**意象锚点**：车间班前会 · 蓝色安全告示板 · 工整白板笔记
- 场景：清晨车间的班前会上，班组长站在蓝色安全告示板前发放培训手册。白色 A4 纸上打印的题目，橘色荧光笔标出重点，配一枚红色印章"合格"。
- 气质关键词：**专业、可靠、克制、可执行**。不是互联网 SaaS 那种轻盈感，而是**工业车间 + 政企告示板**的秩序感。信息优先于装饰，安全高于炫技。
- 对应场景：管理端像"值班室的作战台账"，工人端像"车间墙上一张随手翻的口袋卡片"。

## 视觉策略

- **图像语言**：极少插图。以图标 + 表格 + 数字为主，图标一律 lucide 线性风，1.5px 描边，禁止彩色 3D icon 和渐变 emoji。
- **图形装饰**：允许 1-2px 分隔线、方形/圆角 6px 的信息卡、状态圆点。禁止大面积渐变背景、光晕、玻璃拟态、粒子特效。
- **摄影方向**：不使用真实照片。素材以纯色底 + 图标为准。

## 配色方案

以"工业蓝 + 告示白"为主，橘红作为强调，绿/黄/红作为状态语义色。
- **主色 安全蓝** `#1E5AA8`：来源于车间安全告示板的深蓝，用于导航、主按钮、关键数值。
- **主色-深** `#153F78`：hover / active 态。
- **强调色 提示橘** `#F26E22`：来源于荧光笔勾出的重点，用于"重点"、"待补考"等需要抓眼的位置，用量克制（<10%）。
- **中性 背景白** `#F7F8FA`：告示纸的暖白灰，页面底色。
- **中性 卡面** `#FFFFFF`：卡片、表格、题目容器。
- **中性 边框** `#E4E7EC`：1px 分隔，弱对比。
- **中性 主文字** `#1F2937`：接近黑但不刺眼。
- **中性 次文字** `#667085`：说明、辅助信息。
- **语义 通过绿** `#12A150`：及格、成功、通过状态。
- **语义 警告黄** `#D97706`：临界、补考提醒。
- **语义 危险红** `#DC2626`：不及格、错题、超时、切屏警告。

CSS 变量映射到 shadcn 主题：`--primary: #1E5AA8`；`--destructive: #DC2626`；`--accent`（用于橘色强调）由业务组件单独引用，不覆盖 shadcn 默认 accent，避免污染既有组件。

## 字体排版

- **中文**：`PingFang SC`, `Microsoft YaHei`，走系统栈（layout 已内置）。不引入需外链的中文字体，保证内网/离线可用。
- **西文/数字**：`Inter` 系统等宽 fallback（用 `tabular-nums` 让分数、倒计时对齐）。
- **字号节奏**（8 级）：
  - 页面标题 24 / 600
  - 区块标题 18 / 600
  - 卡片标题 16 / 600
  - 正文 14 / 400
  - 表格/表单 14 / 400
  - 辅助说明 12 / 400
  - 数据大字 32-40 / 700（看板卡片的核心数字）
  - 倒计时 28 / 700 tabular-nums
- **行高**：正文 1.6，标题 1.3。
- **对齐**：数字全部右对齐 + `tabular-nums`；表头左对齐；操作列右对齐。

## 布局与响应式

- **管理后台（PC 优先）**：左侧固定侧边栏（240px），顶部面包屑 + 用户信息条，主内容区最大宽度 1440px 居中，内边距 24-32px。断点 <1024px 时侧边栏收起为 icon-only。
- **工人考试端（H5 优先）**：单列布局，最大宽度 480px 居中，页面内边距 16px，按钮全宽（h-12），字号比 PC 端大一号。禁止水平滚动。
- **考试题目卡**：题干与选项之间 16px 间距，选项之间 12px 间距；选中态使用主蓝色 2px 边框 + 主蓝色 4% 背景。
- **看板**：顶部 KPI 4-6 列自适应卡片，下方图表两列（<1280px 单列）。

## 组件规范

- **shadcn/ui 为唯一基线**：Button / Card / Table / Dialog / Form / Progress / Badge / Tabs 全部走 shadcn 组件。禁止手写重复实现。
- **Badge**：状态语义与颜色一一对应——通过=绿、待补考=橘、不及格=红、未参考=灰。
- **Button**：主操作用 `variant="default"`（安全蓝）；危险操作用 `variant="destructive"`；次要操作用 `variant="outline"`；扫码/下载等辅助操作用 `variant="secondary"`。
- **Table**：斑马纹关闭，行高 48px，鼠标 hover 使用 `#F2F5FA` 弱背景，禁止全边框，保留水平分隔线。
- **卡片**：圆角 8px（--radius 保持默认 0.5rem 即可），阴影仅在 hover 时出现，静态用 1px 边框即可。

## 交互与状态

- **动效性格**：**克制、短促、告知型**。不要弹跳、不要 spring。所有过渡统一 `duration-150 ease-out`，最长 200ms。
- **禁止**：滚动视差、鼠标跟随光晕、卡片翻转、进场逐帧动画。
- **考试答题**：选中选项时选项卡边框 150ms 变色，无缩放/旋转。题目切换用 fade 100ms，不要横向滑动。
- **提交/交卷**：所有关键提交必须有 loading 态（按钮内 spinner）+ 结果 Toast（sonner）。
- **倒计时**：最后 60s 数字变红并轻微加粗，无闪烁。
- **切屏警告**：使用 shadcn Dialog 直接告警，"剩余次数 X/3"，不使用弹幕/浮层。

## 可访问性与文案

- **文案语气**：管理端"专业术语可以用"；工人端**必须口语化**，例如"开始答题"、"再来一次"、"这题选错啦"，避免"提交答卷"这类书面表达。
- **对比度**：主文字 vs 背景 ≥ 7:1；正文 vs 背景 ≥ 4.5:1；禁止使用浅灰字（<`#98A2B3`）作为正文。
- **表单**：必填项标 `*` 且在 label 之后；错误信息用红色 12px 文字紧贴输入框下方，不使用 tooltip。
- **移动端触控**：所有可点击元素最小 44×44px。

## 设计禁忌（明确不要做）

- ❌ **不要**用深色主题作为默认（企业考试要在明亮环境下辨认，暗色反而增加错读）。
- ❌ **不要**用蓝紫渐变、彩色玻璃拟态、AI 生成的科技感背景 —— 这不是 C 端产品。
- ❌ **不要**用大圆角（>12px）和"胶囊"按钮 —— 破坏工业秩序感。
- ❌ **不要**用 emoji 代替状态图标 —— 用 lucide `CheckCircle2` / `AlertCircle` / `XCircle`。
- ❌ **不要**用大面积主色填充背景 —— 主蓝只出现在按钮、导航高亮、关键数值上。
- ❌ **不要**在工人端出现"仪表盘、KPI、率"等 B 端词汇。
- ❌ **不要**为了"美观"隐藏关键数字（分数、剩余次数、题目编号必须始终可见）。
